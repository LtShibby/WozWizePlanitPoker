{
  "name": "wozwize-planit-poker",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "npm-run-all -p dev:server dev:web",
    "build": "npm -w apps/server run build && npm -w apps/web run build",
    "dev:server": "npm -w apps/server run dev",
    "dev:web": "npm -w apps/web run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
